import React from "react";

const Form = () => {
  return (
    <form className="ui form">
      <div className="field">
        <label style={{ fontSize: "1rem" }}>Username</label>
        <input type="text" />
      </div>
    </form>
  );
};

export default Form;
